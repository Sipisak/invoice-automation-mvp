# Only the API is a pnpm workspace member.
# apps/web (Vite + React SPA) is intentionally OUT — kept standalone with its own
# install (apps/web/.npmrc sets ignore-workspace=true). Pivoted from SPFx.
packages:
  - "apps/api"
