# Base URL of the Functions API.
# Local dev: leave UNSET — Vite proxies /api to http://localhost:7071 (see vite.config.ts).
# Production: set to the deployed API, e.g. https://cfig-invoices.azurewebsites.net/api
# VITE_API_BASE=https://your-api.example.com/api
