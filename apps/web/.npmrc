# apps/web is a standalone project, NOT a pnpm workspace member (see root pnpm-workspace.yaml).
# Without this, `pnpm install` here walks up to the root workspace and skips web's deps.
ignore-workspace=true
